import { styled } from "@mui/material/styles";

import Typography from "@Components/UI/Typography";

export const SSectionTitle = styled(Typography)(() => ({
  textTransform: "uppercase",
  marginBottom: "32px",
}));
