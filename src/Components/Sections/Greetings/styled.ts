import { styled } from "@mui/material/styles";

export const SGreetings = styled("section")(() => ({
  padding: "60px 0",
}));

export const SGreetingsList = styled("div")(() => ({
  display: "flex",
  gap: "32px",
}));
